# Model weights (downloaded at runtime via gdown)
models/*.pth
models/*.pt

# Keep the small bundled YOLO base model — needed as fallback
!yolov8n.pt

# Python
__pycache__/
*.pyc
*.pyo
.env

# Streamlit secrets (never commit real secrets)
.streamlit/secrets.toml

# Uploads / temp
static/uploads/
/tmp/

# IDE
.vscode/
.idea/
.idea/